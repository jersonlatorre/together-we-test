{
  "name": "interaction-p5js",
  "version": "1.0.0",
  "description": "proyecto básico con p5js y osc",
  "main": "src/server.js",
  "scripts": {
    "server": "node src/server.js",
    "client": "vite",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "vite build",
    "format": "prettier --write ."
  },
  "dependencies": {
    "express": "^4.18.2",
    "osc-js": "^2.2.0",
    "socket.io": "^4.7.4"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.5.3",
    "vite": "^5.1.4"
  }
}
